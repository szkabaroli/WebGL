{
  "name": "typescript-template",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "watchify -e src/app.ts -p [ tsify --noImplicitAny ] -o dist/bundle.js -v"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^14.4.0",
    "tsify": "^3.0.1",
    "typescript": "^2.3.4",
    "watchify": "^3.9.0"
  }
}
